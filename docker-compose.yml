# data-www
data-www:
    image: busybox
    volumes:
        - ./app:/app
# nginx
mginx:
    image: nginx:alpine
    volumes_from:
        - data-www
    volumes:
        - ./vhosts-main.conf:/etc/nginx/conf.d/default.conf
    ports:
        - "8080:80"
#        - "443:443"
